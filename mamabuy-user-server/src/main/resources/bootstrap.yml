
server:
  port: 8801
spring:
  application:
    name: user-service
  profiles: dev
  cloud:
    config:
      profile: dev
      discovery:
        service-id: config-server
        enabled: true
eureka:
  client:
    service-url: http://106.12.26.193:8888/eureka
logging:
  config: classpath:logback.xml

